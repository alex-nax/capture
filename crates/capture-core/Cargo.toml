[package]
name = "capture-core"
version = "0.1.0"
edition = "2021"
description = "Contract types (serde) for the capture /v1 API + on-disk session formats — the v3 source of truth."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
