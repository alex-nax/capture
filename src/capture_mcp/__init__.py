"""capture-mcp: on-demand capture of a process's window, logs, and audio.

Exposes an MCP server with ``capture_start`` / ``capture_stop`` / ``capture_status``
tools. See :mod:`capture_mcp.server`.
"""

__version__ = "0.2.0"
