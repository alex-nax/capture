{
  "project": "capture-mcp",
  "created": "2026-06-07",
  "notes": "Features 1-15 were built and verified in the initial session (see claude-progress.md). 16+ are open work. Only flip passes->true after verified end-to-end testing.",
  "features": [
    {
      "id": 1,
      "category": "core",
      "priority": "high",
      "description": "MCP server exposing capture_start / capture_stop / capture_status over stdio",
      "acceptance_criteria": [
        "mcp.list_tools() returns the three tools with correct schemas",
        "capture_start requires exactly one of command/pid/app_name"
      ],
      "passes": true,
      "dependencies": []
    },
    {
      "id": 2,
      "category": "core",
      "priority": "high",
      "description": "Launch-mode process capture with timestamped stdout/stderr logging",
      "acceptance_criteria": [
        "Launching a command writes stdout.log, stderr.log, and merged ISO-timestamped output.log",
        "Process exit code is recorded"
      ],
      "passes": true,
      "dependencies": [
        1
      ]
    },
    {
      "id": 3,
      "category": "core",
      "priority": "high",
      "description": "Window discovery via Quartz (pid / app-name -> CGWindowID)",
      "acceptance_criteria": [
        "find_windows() returns layer-0 windows largest-first",
        "primary_window(app_name=...) resolves a running app's main window"
      ],
      "passes": true,
      "dependencies": []
    },
    {
      "id": 4,
      "category": "core",
      "priority": "high",
      "description": "Periodic, grid-scheduled timestamped screenshots (window or whole-screen fallback)",
      "acceptance_criteria": [
        "Screenshots saved as <iso-stamp>.<ext> at the configured interval",
        "Window targeted via screencapture -l when resolvable, else whole-screen",
        "rc=0-but-missing-file is treated as an error (screencapture quirk)"
      ],
      "passes": true,
      "dependencies": [
        3
      ]
    },
    {
      "id": 5,
      "category": "core",
      "priority": "high",
      "description": "Configurable screenshot format, resolution (aspect-preserving), and JPEG quality",
      "acceptance_criteria": [
        "screenshot_resolution accepts 'WxH' and 'WxH/fmt' (e.g. 1280x720/jpg)",
        "Output fits within the box preserving aspect ratio, never upscaled",
        "Format honored (png/jpg/...) via screencapture -t or sips re-encode"
      ],
      "passes": true,
      "dependencies": [
        4
      ]
    },
    {
      "id": 6,
      "category": "core",
      "priority": "high",
      "description": "Audio pipeline: read PCM -> fixed windows -> ASR -> transcript.jsonl/txt with absolute timestamps",
      "acceptance_criteria": [
        "20s of audio chunks into 8+8+4 with monotonically advancing offsets",
        "Timeline anchored to first-byte wall clock (not session t0)",
        "raw audio saved to audio.s16le"
      ],
      "passes": true,
      "dependencies": [
        1
      ]
    },
    {
      "id": 7,
      "category": "asr",
      "priority": "high",
      "description": "Pluggable ASR with local Whisper default (mlx-whisper / faster-whisper)",
      "acceptance_criteria": [
        "asr.create('auto'|'local') returns a working backend on arm64",
        "Real speech transcribes correctly (e.g. 'the quick brown fox ...')",
        "CAPTURE_WHISPER_MODEL env overrides the model"
      ],
      "passes": true,
      "dependencies": [
        6
      ]
    },
    {
      "id": 8,
      "category": "core",
      "priority": "high",
      "description": "Session orchestrator ties process + screenshots + audio with session.json metadata and graceful degradation",
      "acceptance_criteria": [
        "A failed component start rolls back others and re-raises",
        "Audio failure (e.g. -3805) is surfaced in audio_status while screenshots/logs continue",
        "session.json written on start, stop, and error"
      ],
      "passes": true,
      "dependencies": [
        2,
        4,
        6
      ]
    },
    {
      "id": 9,
      "category": "asr",
      "priority": "medium",
      "description": "ScreenCaptureKit Swift helper streams per-app audio as 16kHz mono s16le",
      "acceptance_criteria": [
        "swiftc builds helper/audiocap cleanly",
        "Helper enumerates content, reaches startCapture, prints READY, and emits PCM bytes",
        "--system whole-display mode works; clean exit on SIGTERM/SIGINT"
      ],
      "passes": true,
      "dependencies": []
    },
    {
      "id": 10,
      "category": "core",
      "priority": "high",
      "description": "Async MCP handlers offload blocking work via anyio.to_thread (no event-loop stalls)",
      "acceptance_criteria": [
        "capture_start/stop/status are async and use anyio.to_thread.run_sync",
        "anyio declared as a direct dependency"
      ],
      "passes": true,
      "dependencies": [
        1
      ]
    },
    {
      "id": 11,
      "category": "core",
      "priority": "medium",
      "description": "Bounded session registry with oldest-finished eviction",
      "acceptance_criteria": [
        "_prune_locked keeps running sessions and evicts oldest finished beyond MAX_SESSIONS"
      ],
      "passes": true,
      "dependencies": [
        1
      ]
    },
    {
      "id": 12,
      "category": "testing",
      "priority": "high",
      "description": "Hermetic smoke test suite (tests/smoke.py)",
      "acceptance_criteria": [
        "python tests/smoke.py prints 20/20 passed without special permissions"
      ],
      "passes": true,
      "dependencies": [
        1,
        4,
        6
      ]
    },
    {
      "id": 13,
      "category": "asr",
      "priority": "medium",
      "description": "Remote NVIDIA Riva / Nemotron-3.5 ASR adapter (offline_recognize) with function-id/API-key auth",
      "acceptance_criteria": [
        "Adapter constructs Auth/ASRService and maps RecognitionConfig + word offsets",
        "VERIFIED against a real Riva endpoint returning timestamped transcripts"
      ],
      "passes": false,
      "dependencies": [
        7
      ]
    },
    {
      "id": 14,
      "category": "asr",
      "priority": "medium",
      "description": "ffmpeg microphone fallback verified end-to-end",
      "acceptance_criteria": [
        "audio_source='mic' captures real mic audio and produces a transcript",
        "Graceful message when Microphone permission is missing"
      ],
      "passes": false,
      "dependencies": [
        6
      ]
    },
    {
      "id": 15,
      "category": "infrastructure",
      "priority": "high",
      "description": "Stable code-signing identity so the helper's Screen Recording (TCC) grant persists across rebuilds. scripts/setup_codesign.sh creates the self-signed cert. Session 9: fixed two bugs that made it fail on macOS+OpenSSL-3 — PKCS#12 export now uses -legacy + a non-empty passphrase (empty/AES-MAC p12 fail `security import` MAC verification), and have_identity() detects via `find-identity -p codesigning` WITHOUT -v (a self-signed cert is untrusted so never lists under -v). The skill installer (skills/capture/scripts/install.sh) now runs setup_codesign.sh instead of an ad-hoc build, so skill-based installs get the persistent grant too.",
      "acceptance_criteria": [
        "scripts/setup_codesign.sh creates a codesigning identity and signs the helper (DONE; fixed for OpenSSL 3)",
        "build_helper.sh signs with CODESIGN_IDENTITY (DONE)",
        "Attaching to a playing app yields non-empty transcript.jsonl, no fatal -3805 (DONE via auto-reconnect)",
        "After setup_codesign, helper carries Authority=capture-mcp-codesign (not adhoc) and `audiocap --system` reports READY (DONE; verified on the macOS box Session 9)",
        "skill install.sh stably signs the helper so skill installs also get a persistent grant (DONE)"
      ],
      "passes": true,
      "dependencies": [
        9
      ]
    },
    {
      "id": 16,
      "category": "asr",
      "priority": "medium",
      "description": "True streaming ASR to avoid chunk-boundary word splits (Riva streaming_recognize; overlap+dedup or streaming-capable local backend)",
      "acceptance_criteria": [
        "Words spanning window boundaries are not split or lost",
        "Latency lower than the fixed-window approach"
      ],
      "passes": false,
      "dependencies": [
        6,
        13
      ]
    },
    {
      "id": 17,
      "category": "core",
      "priority": "low",
      "description": "Optional: auto-stop a launch-mode session when the launched process exits",
      "acceptance_criteria": [
        "When the child exits, screenshots/audio stop and state becomes 'stopped' without an explicit capture_stop"
      ],
      "passes": false,
      "dependencies": [
        8
      ]
    },
    {
      "id": 18,
      "category": "infrastructure",
      "priority": "low",
      "description": "Package the helper as a signed .app bundle with NS* usage descriptions for distributable TCC",
      "acceptance_criteria": [
        "Bundled helper requests permissions via its own Info.plist and is notarizable"
      ],
      "passes": false,
      "dependencies": [
        15
      ]
    },
    {
      "id": 19,
      "category": "testing",
      "priority": "medium",
      "description": "CI / pytest harness running the hermetic suite",
      "acceptance_criteria": [
        "pytest green locally; smoke test wrapped as pytest cases"
      ],
      "passes": false,
      "dependencies": [
        12
      ]
    },
    {
      "id": 20,
      "category": "infrastructure",
      "priority": "high",
      "description": "Platform abstraction layer: interfaces (ScreenGrabber/WindowFinder/AudioSource) + a current() factory so OS-specific capture is swappable. macOS code moves behind it unchanged. Design in docs/specs/platform-abstraction.md.",
      "acceptance_criteria": [
        "src/capture_mcp/platform/ with base interfaces + macos backend wrapping today's behavior",
        "session/screenshots/audio/windows call platform.current() instead of importing macOS APIs directly",
        "smoke test still 20/20 on macOS through the abstraction"
      ],
      "passes": true,
      "dependencies": [
        4,
        6,
        8
      ]
    },
    {
      "id": 21,
      "category": "infrastructure",
      "priority": "high",
      "description": "Windows backends: screenshots (Windows.Graphics.Capture or PrintWindow), window discovery (EnumWindows), per-app audio (WASAPI process loopback) emitting the same 16kHz mono s16le contract.",
      "acceptance_criteria": [
        "On Windows, capture_start produces timestamped screenshots of a target window",
        "Per-app (or system) audio yields a transcript; mic fallback via ffmpeg dshow/wasapi",
        "Session output layout + tool params identical to macOS"
      ],
      "passes": true,
      "dependencies": [
        20
      ]
    },
    {
      "id": 22,
      "category": "infrastructure",
      "priority": "medium",
      "description": "Windows bootstrap: init.ps1 equivalent of init.sh (venv, install with platform-gated extras, run smoke). Make pyobjc/mlx optional deps gated by platform.",
      "acceptance_criteria": [
        "init.ps1 sets up a working env on a clean Windows box and runs tests/smoke.py green",
        "pyproject extras gate macOS-only (pyobjc) and arm64-only (mlx) deps by platform"
      ],
      "passes": true,
      "dependencies": [
        20
      ]
    },
    {
      "id": 23,
      "category": "asr",
      "priority": "high",
      "description": "Benchmark local Whisper (faster-whisper CUDA) vs NVIDIA Nemotron-3.5 ASR (via the Riva adapter) on the Windows/NVIDIA box, on identical captured audio. Report WER, real-time factor/latency, GPU memory.",
      "acceptance_criteria": [
        "Both backends transcribe the same audio.s16le clips via the ASRBackend interface",
        "A results doc compares accuracy and speed; verifies the Riva/Nemotron adapter end-to-end (closes #13)"
      ],
      "passes": false,
      "dependencies": [
        13,
        21
      ]
    },
    {
      "id": 24,
      "category": "infrastructure",
      "priority": "medium",
      "description": "Redistributable 'capture' skill (skills/capture/): one-load install + .mcp.json wiring + quick capture actions for any project. Authored/validated with skill-creator.",
      "acceptance_criteria": [
        "install.sh clones+installs capture-mcp (venv, ASR backend, helper) and prints the bin path (DONE)",
        "configure_mcp.py creates/merges .mcp.json preserving other servers (DONE)",
        "set_model.py changes + optionally prefetches the ASR model (DONE)",
        "report_issue.py files a tracked GitHub bug with redacted diagnostics; preview-by-default, posts only with --create + user consent (DONE)",
        ".github/ISSUE_TEMPLATE/bug_report.md provides a structured bug template (DONE)",
        "skill passes skill-creator quick_validate and packages via package_skill.py (DONE)",
        "install.ps1 installs capture-mcp on Windows (find Python, venv, faster-whisper extra, smoke) and prints the bin path; SKILL.md documents macOS+Windows install paths (DONE, Session 9)"
      ],
      "passes": true,
      "dependencies": [
        1
      ]
    },
    {
      "id": 25,
      "category": "core",
      "priority": "high",
      "description": "M0a — engine/MCP package split + SessionRegistry: move engine modules to capture/core (verbatim), thin capture/mcp server; extract the _sessions dict/_prune_locked from server.py into a SessionRegistry that rebuilds history from <output_dir>/capture-*/session.json at startup; fix start() holding session._lock through slow component startup (add a 'starting' state, start components outside the lock, mirroring stop()). See docs/specs/product-architecture.md.",
      "acceptance_criteria": [
        "MCP behavior identical: tools/list schemas unchanged, smoke 20/20 (DONE; suite grew to 28/28)",
        "Server restart no longer loses session history (registry rebuilt from session.json files) (DONE; verified cross-process, Session 10)",
        "capture-mcp console script still works; capture_status responds while another session is starting (DONE; smoke test_status_during_start)"
      ],
      "passes": true,
      "dependencies": [
        8,
        11
      ]
    },
    {
      "id": 26,
      "category": "core",
      "priority": "medium",
      "description": "M0b — EventBus + per-session events.jsonl: components push screenshot_taken/transcript_segment/log_line/state events alongside today's polled counters; CaptureSession appends an append-only events.jsonl per session (state transitions + periodic counter snapshots) so future GUI/daemon clients tail one file instead of fs-watching.",
      "acceptance_criteria": [
        "A smoke capture produces events.jsonl with state transitions and counter snapshots (DONE; starting->running->stopping->stopped + periodic and final snapshots, smoke 35/35)",
        "Existing polling (capture_status/summary()) unchanged (DONE; all pre-existing smoke checks pass untouched)"
      ],
      "passes": true,
      "dependencies": [
        25
      ]
    },
    {
      "id": 27,
      "category": "testing",
      "priority": "high",
      "description": "M0c — contract fixtures + frozen helper contract: tests/contract/ with a golden MCP tools/list schema dump, a recorded StubASR session-dir fixture (session.json + transcript.jsonl + events.jsonl layout), and PCM chunk/offset fixtures for the 8s-window logic; docs/specs/helper-contract.md freezing the audiocap protocol (argv, 16kHz mono s16le PCM on stdout, READY scanned on stderr, -3801/-3803 fatal vs -3805 transient); fix the audiocap.swift:14 'first line is READY' comment lie.",
      "acceptance_criteria": [
        "Contract tests pass against current code and fail on schema/layout drift (DONE; 3/3 hold, injected drift exits 1, --regen path works)",
        "helper-contract.md exists and matches audiocap.swift + audiocap_win.py reality (DONE; also fixed the READY comment lie, the audiocap_win shutdown NameError, and the phantom-watchdog docstring)"
      ],
      "passes": true,
      "dependencies": [
        25,
        26
      ]
    },
    {
      "id": 28,
      "category": "asr",
      "priority": "medium",
      "description": "asr/openai_compat.py — small backend POSTing WAV-wrapped PCM to any OpenAI-compatible /v1/audio/transcriptions endpoint, plus a 'minimal' extra in pyproject.toml (screenshots+logs-only install). Turns the Nemotron WSL2/Docker lab into a plain remote endpoint.",
      "acceptance_criteria": [
        "asr_backend pointing at a compatible endpoint yields timestamped transcript segments (DONE; smoke: direct backend + full AudioCapture pipeline against a stub server, 6 segments at offsets 0.5/2.0/8.5/10.0/16.5/18.0)",
        "pip install 'capture-mcp[minimal]' captures screenshots+logs with no ASR deps (DONE; verified in a fresh uv venv: no mlx/faster-whisper/riva importable, real capture produced screenshots+logs)"
      ],
      "passes": true,
      "dependencies": [
        7
      ]
    },
    {
      "id": 29,
      "category": "core",
      "priority": "medium",
      "description": "list_windows MCP tool — expose WindowFinder discovery (title, app, pid, window id, bounds) so agents get the same window picker the GUI will use.",
      "acceptance_criteria": [
        "tools/list shows list_windows; returns layer-0 windows largest-first on macOS and EnumWindows results on Windows (DONE on macOS: 7 real windows largest-first, app_name filter verified, golden updated; Windows backend goes through the same WindowFinder.find — verify on the Windows box next time it runs the suite)"
      ],
      "passes": true,
      "dependencies": [
        20
      ]
    },
    {
      "id": 30,
      "category": "infrastructure",
      "priority": "high",
      "description": "TCC attribution spike (GATES the daemon architecture): on a clean macOS 14 and 15 VM, verify that a launchd-agent-spawned, Developer-ID-signed PyInstaller daemon is the TCC-responsible process for Screen Recording, that the grant persists across app updates (same Team ID + bundle id), the daemon-restart-after-grant flow, and the macOS 15 periodic re-approval behavior. Document as a repeatable checklist in docs/specs/product-architecture.md.",
      "acceptance_criteria": [
        "Written spike report: attribution works (or the documented fallback decision if it does not) (DONE 2026-06-15, macOS 26.5.1/arm64: PASS — launchd→signed .app PyInstaller daemon→audiocap streamed audio with no terminal grant; spike/tcc-attribution/results/FINDINGS.md)",
        "Grant survives a simulated app update with unchanged identity/bundle id (DONE: same-identity rebuild+re-sign → audio flowed immediately, respawns=0, no re-prompt; identity ROTATION lost the grant — negative control confirms keying to the signing identity)",
        "re-approval / robustness notes recorded (DONE: macOS 26 SCShareableContent enumeration is intermittently flaky — audiocap exit 5 rode by the respawn loop; follow-up logged to add a bounded enumeration retry to audiocap.swift)"
      ],
      "passes": true,
      "dependencies": [
        15
      ]
    },
    {
      "id": 31,
      "category": "infrastructure",
      "priority": "high",
      "description": "M1 — packaged signed engine, no GUI: PyInstaller onedir of the engine+MCP, Developer ID + notarization, PREBUILT signed audiocap (kills the swiftc/Xcode-CLT requirement for end users), capture doctor (permission preflight, CGRequestScreenCaptureAccess trigger, System Settings deep link), distributed via brew tap + GitHub releases.",
      "acceptance_criteria": [
        "brew install <tap>/capture then 'grant once' yields a working audio capture on a machine with no git/Xcode CLT/Python",
        "Packaged-bundle smoke: a real capture runs from the installed artifact",
        "From-source init.sh path still works unchanged",
        "[DONE 2026-06-16] TCC daemon-grant via Developer-ID signing — VERIFIED. The ad-hoc wall: a bundle's separately-signed daemon binary is a different TCC identity than the granted app, so the grant doesn't reach it (confirmed: ad-hoc app granted in Settings, fresh daemon still denied, 0 titles, screenshots fail). FIX (works): sign every binary with Alex's Developer ID (Team YH3QP44ST4) so they share ONE TCC identity. build_macos_dmg.sh CAPTURE_SIGN_IDENTITY path = hardened runtime + packaging/entitlements.plist (allow-jit / allow-unsigned-executable-memory / disable-library-validation / allow-dyld-env for mlx+numba JIT + the .so, + device.audio-input) + secure timestamp, inside-out, helper re-signed with the shared Team ID. VERIFIED: signed+hardened frozen daemon BOOTS; codesign shows Developer ID + flags=runtime; all 4 binaries share Team YH3QP44ST4; after the user grants Screen Recording once, the daemon reports screen_recording=GRANTED and a FRESH daemon (after restart AND app relaunch) keeps it -> 15 windows / 13 titles, real screenshots. The grant PERSISTS (Developer-ID identity is stable, not cdhash) — no re-granting, no daemon crash (the GUI's CGRequest now grants the shared identity). Restarting just the daemon suffices (it's the same identity); the macOS 'restart the app' nudge can be ignored. Notarization (CAPTURE_NOTARIZE_PROFILE=capture-notary, Apple ID pr0fedt@gmail.com stored) in progress for Gatekeeper. STILL TODO for passes:true: notarize + brew tap + prebuilt-helper + capture doctor + from-source-still-works."
      ],
      "passes": false,
      "dependencies": [
        27,
        30
      ]
    },
    {
      "id": 32,
      "category": "core",
      "priority": "high",
      "description": "M2 — captured daemon + /v1 API + CLI: aiohttp HTTP+WS over UDS+bearer token (127.0.0.1 on Windows), endpoint discovery via ~/.capture/daemon.json (0600), /v1 surface per docs/specs/product-architecture.md, daemon-level ASR model pool, capture CLI (start/stop/status/tail/doctor/daemon install|uninstall) writing launchd agent / logon task / systemd user unit; MCP server gains daemon-first mode with embedded fallback (CAPTURE_MCP_EMBEDDED=1).",
      "acceptance_criteria": [
        "Agent-started capture is visible via CLI status; sessions survive client exits (DONE: captured /v1 API on 127.0.0.1+token over a shared SessionRegistry; `capture` CLI + the MCP server (daemon-first) are both clients; smoke test_daemon + test_cli_daemon + test_mcp_daemon_first, 62/62)",
        "One Screen Recording grant (to the daemon) gives working audio from two different terminals' MCP agents (MECHANISM DONE: both terminals' MCP agents proxy to the one daemon via daemon-first dispatch, so they share its registry+grant. The stable-grant BENEFIT still needs the packaged signed launchd daemon #31/#30 to be the TCC-responsible process)",
        "JSON-Schema round-trip contract tests pass; pydantic schema checked in (DONE: daemon/models.py pydantic models = the /v1 contract; v1_schema() + GET /v1/schema; golden tests/contract/golden/v1_schema.json + smoke round-trips live responses through the models + 400 on bad request; registry now returns uniformly full-shaped records so SessionSummary holds. Rust typify generation still planned.)",
        "[DONE] daemon: stdlib /v1 (health/sessions/windows/transcript/admin-shutdown), bearer-token auth (401 without), single-instance guard, ~/.capture/daemon.json (0600); `captured`+`capture` scripts. MCP server: daemon-first dispatch with embedded fallback (CAPTURE_MCP_EMBEDDED=1). docs/specs/daemon.md + mcp-server.md.",
        "[DONE] live event stream GET /v1/events (SSE) fanning each session's EventBus out, tagged with session_id, + `capture watch` CLI; smoke test_sse_events (full lifecycle + log_line/screenshot pushed live); verified with `capture watch` on a real run.",
        "[REMAINING for passes:true] UDS transport (SSE replaces the planned WS for events); daemon lifecycle install (launchd/systemd/logon-task); Rust type generation from /v1 schema; the cross-terminal-AUDIO benefit (needs #31)"
      ],
      "passes": false,
      "dependencies": [
        31
      ]
    },
    {
      "id": 33,
      "category": "gui",
      "priority": "high",
      "description": "M3 — GPUI app v1 on macOS: tray/menu-bar icon (tray-icon+muda), main window (session list from daemon registry, new-capture form with /v1/windows picker), session detail (live preview via RenderImage with cache eviction, transcript/log tails), onboarding (SMAppService daemon registration, Grant flow with permission.updated push, ends with a visible 5-second self-test capture), Settings (ASR model manager with resumable+SHA256 downloads). GPUI pinned to a frozen zed git rev + compatible gpui-component rev, cargo vendor, quarterly bump policy.",
      "acceptance_criteria": [
        "Capture.dmg: drag-install, onboarding to first successful self-test capture without a terminal (PENDING: packaging is M4-adjacent, needs Developer ID #31)",
        "An MCP-agent-started capture appears live in the GUI session list (DONE slice 1: gui/ GPUI app polls /v1/sessions; verified — captures started via the CLI/GUI appear in the GUI list with live shots/segs counts, and history sessions recovered from the disk index show too)",
        "Quitting the GUI does not stop a running capture (DONE: the GUI is a thin daemon client; the daemon owns the session — verified, captures outlive the GUI process)",
        "[DONE slices 1-2, macOS, gpui 0.2.2] gui/: daemon client (health/sessions/windows/start/stop/transcript + /v1/events SSE reader) + GPUI window — health header, /v1/windows picker, Start/Stop, live-polled session list, and a LIVE session-detail pane (screenshot preview via img() + transcript streamed over SSE; auto-opens the running capture). Built+ran+screenshotted: transcript grew live and the preview rendered the video frame. docs/specs/gui.md.",
        "[DONE slice 3] menu-bar (tray) presence via tray-icon 0.24 + muda 0.19 (gui/src/tray.rs): title reflects the running-capture count (● capture / ⦿ N, live-updated from the GPUI poll), Open/Stop-all/Quit menu drained on the main-thread tray loop. Verified: title went ● capture -> ⦿ 1 -> ● capture across a CLI start/stop.",
        "[DONE slice 4] global hotkey ⌃⌘R (global-hotkey 0.8, Carbon — no accessibility prompt) toggles capture from anywhere (running -> stop all; else start on selected window), drained in the tray loop. Registration verified (GUI shows the '⌃⌘R toggles capture' hint = manager+register succeeded); the live key-press path needs a real hardware press (synthetic keystrokes need Accessibility / don't trigger Carbon hotkeys).",
        "[DONE slice 5] GUI installs/updates the bundled `capture` skill into a coding agent's home (~/.claude/skills/capture, ~/.codex/skills/capture) with a content-hash status check (— install / ✓ / ↑ update); verified via --skill-status/--install-skill headless flags (not installed→up to date→update available→up to date).",
        "[DONE slice 6] packaging/build_macos_dmg.sh builds Capture.app (ad-hoc signed, bundles the skill) + Capture-<v>.dmg for macOS testing; README documents the Gatekeeper bypass (right-click Open / Open Anyway / xattr -dr quarantine). NOT notarized — Developer ID is #31.",
        "[DONE slice 7] self-contained bundle: build_macos_dmg.sh now PyInstaller-freezes the daemon into Capture.app/Contents/Resources/captured/ (with the signed audiocap helper beside it, keeping its stable com.local.audiocap identity), and the GUI auto-spawns it (daemon.rs bundled_daemon()/spawn_detached(), called from app.rs new(); poll+SSE re-discover so they attach to the just-spawned daemon). No separate venv / `capture daemon start`. Verified: the frozen daemon copied OUT of the .app boots, /v1/health ok, /v1/windows returns windows (Quartz works frozen); app signature verifies --strict; DMG is 28M. Transcription still needs a configured ASR backend (mlx excluded to keep the bundle small).",
        "[DONE slice 8] in-GUI Whisper model manager + on-device ASR in the bundle: daemon /v1/asr/* routes (GET models catalog with downloaded/active/downloading flags; POST models/download -> background fetch with asr_download progress over SSE + dup-guard; POST model -> set active, persisted to ~/.capture/config.json via core.config). New core.asr.manager (verified mlx-community repos: whisper-tiny, whisper-base-mlx, whisper-large-v3-turbo-q4, whisper-small-mlx, whisper-medium-mlx, whisper-large-v3-turbo) + whisper_local reads the config. GUI: a Whisper-models panel (Download/Use buttons, live ↓NN% from SSE asr_progress; asr_download events have NO session_id so SSE handles them before the session filter). build_macos_dmg.sh now BUNDLES mlx (de-risked: frozen Metal kernel + whisper-tiny transcribe OK; --asr-selftest in captured_main); freeze_support() prevents a rogue 2nd daemon (numba multiprocessing). DMG ~166M (mlx metallib ~125M); weights download on demand. Verified live: routes + SSE progress (whisper-base-mlx 0->1->done), config persist, bad/dup handling; bundled daemon reports backend_available:true, single process. Contracts regenned (3 ASR models added), smoke 68/68.",
        "[DONE slice 9] native macOS menu-bar agent (agent/macos/CaptureBar.swift, NSStatusItem + LSUIElement): the bundle's entry point (CFBundleExecutable=CaptureBar). Owns the PERSISTENT menu-bar presence (○/●/⦿N reflecting daemon+capture state, polled /v1 every 2s), the daemon lifecycle (spawns the bundled captured detached; Quit gracefully /v1/admin/shutdown the daemon IF idle, freeing the .app to delete/replace), and launches the GPUI window (capture-gui with CAPTURE_AGENT=1 -> skips its own tray + daemon-spawn, exits on window-close via on_window_closed). Why a separate native agent: gpui 0.2.2 forces ActivationPolicy::Regular (no LSUIElement menu-bar mode) and a resident GPUI process is too heavy for an always-on tray; owner chose native per-OS agents (Windows sibling = #36). build_macos_dmg.sh compiles it (swiftc) + lays out MacOS/{CaptureBar,capture-gui}. Verified: launched via LaunchServices it stays resident, spawns the daemon (/v1/health ok), and opens the window; docs/specs/agent.md.",
        "[DONE slice 10] GUI usability batch (Alex, from the running app): (1) Windows picker + (2) Sessions lists show ALL rows (dropped the top-6 slices) in a single scrolling page with a CUSTOM DRAGGABLE SCROLLBAR (scrollbar()+on_scrollbar_drag() overlay from the root ScrollHandle's bounds/max_offset/offset) — bare gpui 0.2.2 has no scrollbar widget and nested overflow_scroll regions fight the root for the wheel, so a single scroll context is used (first tried per-list max_h+overflow_y_scroll: scrolled-together + no scrollbar, per Alex; reverted); (3) Launch-and-capture a new process/URL from a minimal single-line text input (focusable div + track_focus/on_key_down: key_char/backspace/⌘V-paste/Enter; no IME) -> POST /v1/sessions with command (a URL is e.g. `open https://…`); (4) per-session Folder button (open the dir in Finder via `open`); (5) per-session Copy-Prompt button writes a ready-to-paste summarization prompt (points a coding agent at the dir's transcript/screenshots/logs) to the clipboard; (6) per-session Delete (finished only) -> new daemon POST /v1/sessions/{id}/delete (registry.delete + rewrite sessions.jsonl index + rmtree the dir guarded by session.json presence; 400 if live). Verified: delete route live-tested (running->400, stopped->200 + dir gone + de-indexed + 404), smoke 68/68, contracts 4/4, GUI builds clean. The text-input + button UI is a manual visual check (no GPUI harness). NOTE: open-folder uses macOS `open` (Windows/Linux branch owed).",
        "[DONE slice 11] (Alex, from testing) (a) Sessions list was incomplete: the registry rebuilt history from the index (sessions.jsonl) only, so capture-* folders predating an index reset were orphaned/invisible. registry._load_history now also scans $CAPTURE_RUNS_DIR (else ~/.capture/runs) for capture-*/session.json and recovers any uncovered sid (index wins; idempotent; re-bounds to max_sessions) — verified 8->15. (b) Window rows showed 'App — ' because macOS redacts kCGWindowName without Screen Recording; GUI shows just the app name when title is empty. (c) Permissions UI: new core.permissions (Quartz CGPreflight/CGRequestScreenCaptureAccess) + daemon GET /v1/permissions, POST /v1/permissions/request; GUI Permissions row shows Screen Recording status (polled) + Grant (daemon triggers the system prompt; the daemon is the grantee; needs app restart to apply) + Open Settings (x-apple.systempreferences ScreenCapture pane for grant OR revoke — apps can't toggle TCC). Mic via AVFoundation deferred (not in venv). Verified: routes live (status granted/denied, bad kind->400), smoke 68/68, GUI clean. Needed a daemon RE-FREEZE to ship the Python changes in the bundle.",
        "[DONE slice 12] permissions UI completion (Alex: \"we need both\"): (a) MICROPHONE permission — added pyobjc-framework-AVFoundation (pyproject + freeze --collect-all AVFoundation/CoreMedia; works frozen, verified mic status != unknown in the bundled daemon); core.permissions gained microphone_status/request_microphone (AVCaptureDevice authorizationStatus/requestAccess) + a request(kind) dispatcher; daemon GET /v1/permissions now returns screen_recording + microphone; POST request handles kind=microphone. GUI: a per-permission perm_row (status + Grant + Settings) for Screen Recording AND Microphone (Settings -> Privacy_ScreenCapture/Privacy_Microphone). (b) AUTO-RESTART after a grant — a new Screen Recording grant needs the daemon to restart; GUI 'Restart daemon' POSTs /v1/admin/shutdown and the menu-bar AGENT auto-respawns it (CaptureBar poll: if daemon down AND weStartedDaemon -> ensureDaemon, debounced 6s via lastSpawn; intentional Stop-Daemon clears weStartedDaemon so it isn't respawned). No app quit/reopen. Verified: routes live (sr+mic, bad kind->400), bundled daemon serves both frozen, smoke 68/68, GUI+agent compile clean. Mic grant applies immediately; SR needs the restart.",
        "[DONE slice 13] FIX: clicking 'Grant' for Screen Recording CRASHED the daemon (Alex: 'permission request failed: Unexpected EOF' + 'no daemon'). Root cause: CGRequestScreenCaptureAccess requires a window-server connection and SIGABRTs a headless process; the daemon called it. Fix: the daemon NEVER prompts for screen recording — request_screen_recording() returns status only; the GUI shows the prompt itself via CoreGraphics FFI (screen_perm::request() -> CGRequestScreenCaptureAccess; the GUI is a real app, won't crash). Microphone stays in the daemon (requestAccessForMediaType is dispatch-based, headless-safe — verified). Also made the agent's auto-respawn robust: gated on userStoppedDaemon (set by Stop Daemon) instead of weStartedDaemon, so it recovers regardless of how the daemon started (the 'GUI fails to reconnect on restart' report was the daemon being down from the crash; the poll re-discovers fine once it's back; verified kill-9 -> respawn in ~2s). Verified: daemon request('screen_recording') survives (no crash); smoke 68/68; GUI+agent compile clean; re-froze the bundle.",
        "[DONE slice 14] (from install testing) (a) FIX: Microphone 'Grant' ALSO crashed the daemon — AVCaptureDevice.requestAccessForMediaType aborts a headless/backgroundonly process too (not just CGRequest; the earlier test was in an already-granted context). Mic now mirrors screen: daemon returns status without prompting; the GUI mic row has NO Grant button (no block-free Rust prompt) — only Settings; macOS auto-prompts mic the first time the ffmpeg fallback opens the device. Screen still prompts via the GUI's CoreGraphics FFI. (b) FIX: no visible menu-bar tray on launch — the agent's text label (○ capture) was too easy to miss; replaced with an SF Symbol ICON (applyIcon: record.circle / record.circle.fill + running count; text fallback) so it's findable in a crowded/notched menu bar. Verified: daemon mic/screen requests survive (return status, no crash); reinstalled to /Applications and launched (agent+daemon+gui up, healthy); smoke 68/68; GUI+agent clean.",
        "[DONE slice 15] (Alex) menu-bar 'Open Window' now FOCUSES the existing GUI window (agent tracks guiProcess; if isRunning -> NSRunningApplication.activate) instead of launching a duplicate; only launches a fresh one when none is open. Agent compiles clean.",
        "[DONE slice 16] Microphone Grant restored (Alex: 'cannot request it myself') now that Developer-ID signing works: the GUI mic 'Grant' spawns the bundled agent one-shot (CaptureBar --request-mic -> Swift AVCaptureDevice.requestAccess, waits for the answer, exits); shares the Team ID so the grant reaches the daemon. request_permission(kind) dispatches screen (CoreGraphics FFI) vs mic (agent one-shot); both perm rows promptable again. GUI+agent compile clean.",
        "[DONE slice 17] (Alex: 'settings should not bloat main screen') Settings screen: a ⚙Settings/←Back toggle (show_settings) splits the window into the capture dashboard (Refresh/Start, Launch input, windows/sessions, live detail) and a Settings screen holding Capture quality + Whisper model manager + Permissions + skill installer (each panel via settings.then()/(!settings).then() so only one screen's panels render). NEW Capture quality: chip toggles for screenshot format (PNG/JPEG, shot_format), resolution (RES_PRESETS native/1440/1080/720, shot_res_ix), JPEG quality (60/80/95 when jpeg) — merged into the /v1/sessions body via shot_settings() so new captures use them (existing daemon StartSessionRequest fields; no daemon change). GUI builds clean. NOT yet deployed — Alex has a live meeting transcription on the running daemon; will rebuild+reinstall after.",
        "[REMAINING for passes:true] RenderImage with eviction for the preview (img() cache leak on long runs); a custom branded menu-bar glyph (SF Symbol icon is done); global hotkey moved into the agent (works window-less); delete-confirm + bulk/clear-all captures; onboarding (SMAppService daemon/login-item registration + self-test capture); offline transcription polish (avoid hf revision-check on cached models); the rest of M1 #31 (brew tap, prebuilt helper, capture doctor; TCC daemon-grant via Developer-ID is DONE + notarization working); migrate gpui 0.2.2 -> pinned zed git rev for Linux/a11y"
      ],
      "passes": false,
      "dependencies": [
        32
      ]
    },
    {
      "id": 34,
      "category": "gui",
      "priority": "medium",
      "description": "M4 — Windows release: GPUI app on the DX11 backend, Inno Setup installer (+ winget), logon-task daemon install, interactive-station preflight UX, and per-process audio via a NATIVE helper binary (windows-rs, ActivateAudioInterfaceAsync + PROCESS_LOOPBACK with INCLUDE_TARGET_PROCESS_TREE for Chromium-family apps) speaking the frozen 16kHz s16le PCM stdout contract — closes the per-process half of #21. Ship with honest SmartScreen messaging (Trusted Signing unavailable to individuals; OV cert does not clear cold-start). SPEC: docs/specs/windows-release.md. Decisions (2026-06-17, owner): Inno Setup + winget; logon task (not a Service); don't bundle CUDA (CPU int8 / remote openai-compat / Riva / screenshots-only minimal alternatives for non-NVIDIA boxes); unsigned v1 with a signtool hook (CAPTURE_WIN_SIGN_*); cross-platform in-app auto-update (generalize gui/src/update.rs + a PowerShell updater; both .dmg and .exe under one release tag). [2026-06-17] Per-process audio NATIVE helper landed + dev-verified: helper/audiocap_win_rs (Rust windows-rs; ActivateAudioInterfaceAsync + AUDIOCLIENT_ACTIVATION_TYPE_PROCESS_LOOPBACK + INCLUDE_TARGET_PROCESS_TREE) -> audiocap_win.exe, wired into Win32AudioSource (prefers it when a target pid is known; Python system-loopback fallback with the parents[3]->[4] path bug fixed). Verified: standalone capture rms ~2113 + integrated daemon audio.s16le rms ~1526; smoke 67/67. [2026-06-17] INSTALLER LANDED + dev-verified: packaging/build_windows.ps1 (PyInstaller freeze of the daemon — faster-whisper CPU, no CUDA libs — + Inno Setup packaging/capture.iss) -> CaptureSetup-0.2.5-x64.exe (74MB). Silent install lays out the full tree (agent+GUI+frozen daemon+native helper+skill+logon-task script); the frozen daemon serves /v1/health (platform=win32, SAC does not block it); uninstall clean. [2026-06-17 install feedback] Fixed 2 issues from a real Windows install: (1) a console window whose close killed the app — gui + agent main.rs now carry #![cfg_attr(not(debug_assertions), windows_subsystem=windows)] so RELEASE builds are GUI apps with no console (installer must ship release binaries); (2) 'Whisper runtime unavailable in this daemon' — manager.runtime_available()/catalog() are now runtime-aware (faster-whisper Systran/* catalog on Windows, not mlx-only) and FasterWhisper reads the GUI-persisted whisper_model config. Verified backend_available=True + Systran/faster-whisper-* catalog on the box. [2026-06-17] AUTO-UPDATE cross-platform: gui/src/update.rs now picks the OS asset (.dmg/macOS, CaptureSetup*.exe/Windows) and installs it — install_windows downloads the .exe, runs a detached PowerShell updater (stop Capture/capture-gui/captured -> installer /VERYSILENT in-place upgrade -> relaunch). GUI builds clean. Verifying needs a newer release to update to (done in the release flow, no throwaway version). Remaining for passes:true: cut a release (bump once) with both .dmg + .exe assets, verify the update both ways, + Authenticode signing + multi-app audio isolation A/B + GUI source labeling.",
      "acceptance_criteria": [
        "CaptureSetup-x64.exe installs and first capture works without a terminal",
        "Per-process loopback isolates a Chromium app's audio while other apps play sound",
        "GUI labels system-mix vs per-process source correctly",
        "Core-portability prereqs fixed so the daemon runs on Windows (cli start uses creationflags, not start_new_session; vision indexing downscales via Pillow; capture_import degrades cleanly)",
        "Non-NVIDIA Windows ASR works (CPU int8 or a configured remote endpoint) with no hard failure when CUDA is absent",
        "In-app auto-update on Windows downloads the installer, runs it silently, and relaunches; the same release tag carries both the macOS .dmg and the Windows .exe"
      ],
      "passes": false,
      "dependencies": [
        21,
        27,
        33
      ]
    },
    {
      "id": 35,
      "category": "infrastructure",
      "priority": "medium",
      "description": "M5 — Linux: third platform backend (X11 window discovery + screenshots first; Wayland via xdg-desktop-portal ScreenCast — picker-only targeting, persistent share indicator, re-persisted restore tokens, documented app_name degradation; PipeWire per-app audio with a reconnect-tracking helper using -3805-style semantics), systemd user unit, then GPUI app on wgpu (Vulkan requirement documented; engine artifact must not link GPUI so headless/SSH agents keep working).",
      "acceptance_criteria": [
        "MCP agent captures screenshots+logs on X11 Linux with no GUI installed",
        "Wayland capture works through the portal flow with documented semantics",
        "PipeWire per-app audio yields a transcript for a target app while another app plays sound"
      ],
      "passes": false,
      "dependencies": [
        20,
        32
      ]
    },
    {
      "id": 36,
      "category": "gui",
      "priority": "medium",
      "description": "Windows menu-bar/tray agent — the native sibling of the macOS CaptureBar (docs/specs/agent.md). A thin always-resident system-tray app in RUST (windows-rs + tray-icon/muda, owner's decision 2026-06-17) that owns the persistent tray icon, the daemon lifecycle (spawn the bundled captured with CREATE_NO_WINDOW; stop it on Quit if idle), and launches the GPUI window (capture-gui with CAPTURE_AGENT=1) on demand. Same role + same /v1 contract as the macOS agent; no shared code (native per-OS). Part of the M4 Windows release. SPEC: docs/specs/agent-windows.md. [2026-06-17] LANDED + dev-verified: agent/windows (Rust; tray-icon + muda + a minimal Win32 message loop) -> Capture.exe — stays resident, adopts/spawns the daemon (CREATE_NO_WINDOW), launches capture-gui.exe (CAPTURE_AGENT=1); + packaging/register_logon_task.ps1 (interactive logon task, verified register/unregister, no admin). Verified: agent_alive + gui_count=1 + daemon adopted. Remaining for passes:true: packaged install + manual tray-icon/menu pass + survives-window-close (M4); Open-Window focus of an existing window is a TODO.",
      "acceptance_criteria": [
        "Launching the packaged Windows app shows a persistent system-tray icon that reflects daemon/capture state and survives closing the window",
        "The tray spawns the bundled daemon on launch and stops it on Quit (so the install isn't pinned)",
        "Tray 'Open Window' launches the GPUI window (CAPTURE_AGENT=1) which skips its own tray and exits on close"
      ],
      "passes": false,
      "dependencies": [
        33,
        34
      ]
    },
    {
      "id": 37,
      "category": "gui",
      "priority": "medium",
      "description": "Grouped multi-window/multi-app capture picker + microphone selector. The GUI window picker groups windows by application with checkboxes (multi-select across several apps at once); 'Start capture' spawns one /v1/sessions per checked window (per-window screenshots via window_id), deduping app audio per process so only the first checked window of each app records the app audio (macOS audio is per-application, not per-window). A mic selector (GET /v1/audio/mics; devices from the bundled audiocap helper's --list-mics, AVFoundation AVCaptureSession — NO ffmpeg) attaches ONE chosen input device to ONE app via a per-app mic radio; the mic records as a SEPARATE track (mic.s16le / mic_transcript.jsonl), never mixed with app audio. mic_device persists in gui-settings.json. Daemon: StartSessionRequest.mic_device + GET /v1/audio/mics; CaptureSession runs a second AudioCapture(track='mic'); helper gains --mic <id|default> and --list-mics.",
      "acceptance_criteria": [
        "The window picker groups windows under per-app headers with a checkbox per window; windows from multiple apps can be checked simultaneously.",
        "Start capture spawns one session per checked window; screenshots are per-window (correct window), and only one session per app process captures app audio.",
        "A mic selector lists input devices from GET /v1/audio/mics and lets the user pick one device + one target app (mic radio); that app's session writes mic.s16le + mic_transcript.jsonl as a separate track.",
        "No ffmpeg dependency: mic capture + device enumeration go through the bundled audiocap helper (AVCaptureSession).",
        "mic_device selection persists across GUI relaunch (gui-settings.json)."
      ],
      "passes": true,
      "dependencies": [
        33
      ]
    },
    {
      "id": 38,
      "category": "audio",
      "priority": "medium",
      "description": "Microphone echo/noise cancellation (without breaking playback). When a mic track is recorded alongside app audio on a device with speakers (a laptop), the built-in mic picks up the speaker output = acoustic echo. First attempt used the macOS voice-processing unit (AVAudioEngine setVoiceProcessingEnabled) which DOES echo-cancel, but it puts the system into a communication audio mode that ducks/mutes other apps' output (user can't hear the captured app) and over-cancels the mic to silence — REVERTED. #37's mic now uses plain AVCaptureSession; headphones are the interim workaround. A proper fix must remove speaker bleed from the mic track WITHOUT affecting playback — e.g. offline/streaming NLMS adaptive echo cancellation using the already-captured app audio (audio.s16le) as the reference signal, or a non-invasive AEC config that doesn't engage system audio ducking. Optionally noise suppression for the mic.",
      "acceptance_criteria": [
        "With a mic track + app audio on laptop speakers, the mic transcript contains the user's voice but NOT the app's audio (no echo).",
        "App audio playback is unaffected — the user still hears the captured app normally while recording.",
        "Works for the built-in mic; degrades gracefully (plain capture) if cancellation can't run."
      ],
      "passes": false,
      "dependencies": [
        37
      ]
    },
    {
      "id": 39,
      "category": "gui",
      "priority": "medium",
      "description": "Session-list UX: SVG icon buttons (Lucide, via a gpui AssetSource in gui/src/assets.rs + assets/icons/*.svg) replacing text/emoji on the session-row actions (folder/clipboard/stop/trash) and the mic radio; a DELETE CONFIRMATION modal (occluding backdrop + centered Cancel/Delete card); and a SESSION PLAYBACK SCREEN (third top-level screen with a Back button). Opening a session shows the latest screenshot + time-synced subtitles; finished captures get a video-style SCRUBBER (draggable timeline + transport: start/-5s/play-pause/+5s/end + mm:ss) that moves the screenshot + active subtitle through time, plus auto-play. GUI reads screenshots/ + transcript.jsonl + mic_transcript.jsonl from disk (ISO timestamps parsed to epoch); no daemon change.",
      "acceptance_criteria": [
        "Session-row action buttons and the mic selector render proper SVG icons (no emoji/text labels).",
        "Clicking the delete (trash) icon opens a confirmation modal; Cancel dismisses, Delete removes the session.",
        "Clicking a session opens a Playback screen with a Back button; running sessions show the live screenshot+transcript.",
        "For a finished capture, a scrubber + transport controls move a screenshot and its time-synced subtitle through the timeline; play/pause auto-advances."
      ],
      "passes": true,
      "dependencies": [
        33,
        37
      ]
    },
    {
      "id": 40,
      "category": "gui",
      "priority": "low",
      "description": "Session pruning + capability status icons. Daemon POST /v1/sessions/{id}/prune {parts}: delete all screenshots, HALVE the screenshot cadence (drop every other frame), and/or remove the audio stream (audio.s16le/mic.s16le). SessionSummary gains capability flags (has_screenshots, has_audio, has_mic, can_retranscribe) updated on prune; the GUI shows status icons per session + prune controls, warning that removing audio disables re-transcribe (#41). Saves disk on big sessions.",
      "acceptance_criteria": [
        "A prune action can delete all screenshots, halve the screenshot count (every other frame), or remove the audio stream.",
        "The session summary exposes capability flags reflecting what remains after pruning.",
        "The GUI shows per-session status icons and disables actions that pruning made impossible (e.g. re-transcribe when audio removed)."
      ],
      "passes": true,
      "dependencies": [
        39
      ]
    },
    {
      "id": 41,
      "category": "asr",
      "priority": "low",
      "description": "Re-transcribe a saved session with a chosen/again model. Daemon POST /v1/sessions/{id}/retranscribe {asr_backend?, model?} reads audio.s16le, re-chunks it (reuse the audio.py windowing), runs the selected ASR backend/model, and rewrites transcript.jsonl/.txt. Gated on the can_retranscribe capability (audio still present). Lets a user upgrade an old transcript with a more powerful Whisper model.",
      "acceptance_criteria": [
        "POST retranscribe on a session with audio produces a fresh transcript using the requested model.",
        "Attempting it on an audio-pruned session is rejected (can_retranscribe=false)."
      ],
      "passes": true,
      "dependencies": [
        40
      ]
    },
    {
      "id": 42,
      "category": "gui",
      "priority": "medium",
      "description": "Toggle visual (screenshot) capture on/off. A GUI control to start a capture with screenshots DISABLED (audio-only session) — the daemon already accepts capture_screenshots at start, so this is a small start-option toggle — and, as a follow-on, pause/resume screenshots MID-session (needs a new daemon endpoint to start/stop the Screenshotter on a live session).",
      "acceptance_criteria": [
        "A GUI toggle starts a capture with no screenshots (audio + transcript only); the session reflects capture_screenshots=false.",
        "(stretch) Screenshot capture can be paused/resumed on a running session."
      ],
      "passes": true,
      "dependencies": [
        33
      ]
    },
    {
      "id": 43,
      "category": "engine",
      "priority": "medium",
      "description": "Import an existing audio or video file as a session. Audio: read the file, resample to 16 kHz mono s16le, run ASR → a normal session with transcript.jsonl/.txt (+ the raw audio). Video: ALSO extract periodic frames as screenshots/ and the audio track for transcription, so external recordings get the same session artifacts (playback scrubber, prompts, search). Frame + audio extraction must avoid ffmpeg (the project's no-ffmpeg rule) — use AVFoundation in a Swift helper (AVAssetImageGenerator for frames, AVAssetReader for audio). New POST /v1/sessions/import {path} or capture_import MCP tool + GUI 'Import…' control.",
      "acceptance_criteria": [
        "Importing an audio file produces a session with a transcript using the active model.",
        "Importing a video file produces a session with timestamped screenshots (extracted frames) + a transcript of its audio.",
        "No ffmpeg dependency — extraction goes through the bundled helper (AVFoundation)."
      ],
      "passes": true,
      "dependencies": [
        39
      ]
    },
    {
      "id": 44,
      "category": "indexing",
      "priority": "medium",
      "description": "Hierarchical multimodal index of a session's screenshots via a REMOTE multimodal LLM (LM Studio on the LAN, 192.168.31.217:1234, Qwen vision model, OpenAI-compatible /v1/chat/completions). Divide-and-conquer over the timeline: build a balanced binary tree by midpoint splits, vision-caption the LEAF frames, then conquer by combining children up to a root summary. Each node stores metadata + RAW artifacts (time range, representative frame, raw vision caption, raw time-aligned transcript slice, fused summary, parent/child links) so the session is readable at any zoom level and re-combinable without re-running vision. Leaf frames chosen by a tunable index sampling rate (0<rate<=1, default 0.5 = every other frame) with a max_leaves backstop; the capture interval is also configurable (down to 0.5s). Transcript fused into the combine step when present. DISABLED BY DEFAULT — enabled only when a working LM Studio endpoint is configured (URL set + reachable preflight); no frames are sent otherwise. Resumable background job + SSE progress (the run is lengthy and the LAN server may drop). Stdlib-only OpenAI-compatible vision client. New POST /v1/sessions/{id}/index + GET .../index + capture_index MCP tool + GUI Index action with a progress tree. Spec: docs/specs/indexing.md (APPROVED — D0–D5 resolved).",
      "acceptance_criteria": [
        "Indexing a finished session builds index.json: a balanced binary tree of nodes, each with time range + representative frame + raw vision caption + raw transcript slice + fused summary, and a root whole-session summary.",
        "The run streams progress over SSE (descend/ascend phases, nodes_done/total) and checkpoints so a dropped LAN connection resumes rather than restarts.",
        "Leaf count is bounded by the tunable sampling rate + max_leaves cap; a session without screenshots is rejected (can_index=false).",
        "Indexing is disabled unless a working LM Studio endpoint is configured (index_available); the route 400/503s and the GUI control is hidden otherwise.",
        "Reaches both surfaces: daemon endpoint + Python client + capture_index MCP tool + Rust client + GUI control (parity)."
      ],
      "passes": true,
      "dependencies": [
        40,
        43
      ]
    },
    {
      "id": 45,
      "category": "asr",
      "priority": "high",
      "description": "Transcription quality + settings. Root cause of garbage transcripts (e.g. Russian audio → repeated 'Thank you.'): Whisper hallucinates phantom phrases on SHORT chunks (old 8s default) and on SILENCE (a dead mic looped junk). Fixes: (1) hallucination guards in the mlx + faster-whisper backends (condition_on_previous_text=False, no_speech/logprob/compression thresholds); (2) an RMS silence-gate (asr.is_silent) that skips near-silent chunks in audio.py + retranscribe.py; (3) ASR LANGUAGE as a persisted, on-the-fly SETTING (manager.active_language; POST /v1/asr/language; client; MCP transcription_settings + capture_retranscribe language arg; GUI field in Settings + the live playback pane — a running capture's next chunk picks it up); (4) chunk/sample LENGTH as a persisted SETTING (default 30s, manager.active_chunk_seconds; POST /v1/asr/chunk; GUI chips; StartSessionRequest chunk default None→config; re-transcribe uses the current setting not the old session's 8s). Re-transcribe accepts language + chunk_seconds overrides to fix an existing wrong-language transcript.",
      "acceptance_criteria": [
        "A non-English capture transcribes correctly instead of repeated 'Thank you.' (verified: a Russian session re-transcribed at 30s + language=ru produced 219 coherent segments vs 18 'Thank you.').",
        "A silent/dead mic produces no hallucinated text (silence-gated).",
        "Language + chunk length are persisted settings reachable from the daemon, the GUI (Settings + live pane), and MCP (transcription_settings); a language change applies to a running capture on the fly.",
        "Re-transcribe can fix an existing session with a chosen language/chunk length."
      ],
      "passes": true,
      "dependencies": [
        41
      ]
    },
    {
      "id": 46,
      "category": "engine",
      "priority": "medium",
      "description": "Switch the active microphone on a LIVE capture without restarting. CaptureSession.set_mic_device(device): stop the running mic AudioCapture, start a new one with the new device, appending to mic.s16le / mic_transcript.* so the recording stays continuous (each capture's epoch is real wall-clock, so iso timestamps align). device id / 'default' = on/switch, null/'' = off. AudioCapture gained an append mode. Surfaces (parity): POST /v1/sessions/{id}/mic {device}; DaemonClient.set_mic; MCP capture_set_mic; GUI live Mic chip row in the playback pane (running sessions) that highlights the active mic_device (now in the summary).",
      "acceptance_criteria": [
        "On a running capture, switching the mic device changes the recorded input live and appends to the mic track (verified: switch ON created mic.s16le, switch OFF set mic_status=off).",
        "Switching on a non-running session is rejected (400); unknown session 404.",
        "Reaches the daemon, the Python client, the capture_set_mic MCP tool, and the GUI (parity)."
      ],
      "passes": true,
      "dependencies": [
        8
      ]
    },
    {
      "id": 47,
      "category": "skill",
      "priority": "low",
      "description": "Keep the bundled `capture` skill current + suggest updating it. Updated skills/capture/SKILL.md + references/quick-actions.md to document the full tool set (list_windows, list_audio_devices, capture_set_mic, capture_prune, capture_retranscribe, transcription_settings, capture_import, capture_index) + the new capture_start options (window_id, mic_device, capture_screenshots) and the hallucination/transcription-settings guidance. The GUI Settings already detects a stale install via a content hash (skill::status → SkillStatus::UpdateAvailable, shown as '↑ update'); editing the bundled skill makes installed copies surface that suggestion. The DMG build bundles the skill into Resources/skill.",
      "acceptance_criteria": [
        "The bundled skill documents every current MCP tool + capture_start option.",
        "Settings shows '↑ update' for an installed skill that differs from the bundled one (verified: edited skill differs from the installed copy)."
      ],
      "passes": true,
      "dependencies": []
    },
    {
      "id": 48,
      "category": "gui",
      "priority": "medium",
      "description": "In-app auto-update from GitHub releases, with confirmation. gui/src/update.rs: at startup check() GETs github.com/repos/alex-nax/capture/releases/latest, semver-compares tag_name to the running CARGO_PKG_VERSION, and finds the .dmg asset; Settings shows 'vX · up to date' or 'vY available → Update…'. Update goes through the shared confirm modal (ConfirmKind::Update); ONLY on confirm, download_and_install fetches the notarized dmg and spawns a detached updater script that quits the app + daemon, mounts the dmg, replaces /Applications/Capture.app, strips quarantine, and relaunches. Never installs without confirmation; same Developer-ID signature preserves the TCC grant.",
      "acceptance_criteria": [
        "On startup the app checks GitHub and, when a newer release exists, surfaces it in Settings (verified against the real API: 0.2.0 vs v0.2.0 → no offer; the .dmg asset URL is correctly resolved).",
        "Installing requires explicit confirmation (a modal); the updater replaces /Applications/Capture.app and relaunches.",
        "No update is offered or installed when already on the latest version."
      ],
      "passes": true,
      "dependencies": [
        33
      ]
    },
    {
      "id": 49,
      "category": "indexing",
      "priority": "high",
      "description": "Resolution-adaptive index max_px. Study (daf420, controlled): same frames/prompt, max_px 1024->2048 lifted UE C++ code_fidelity 0.42->0.88, filename accuracy 0.30->0.95, hallucination 0.55->0.10 — resolution is THE lever for systematic small-font OCR (sampling density is not). vision_client.structured_image gains a per-call max_px override; the AUTO path bumps code/terminal leaves to CAPTURE_INDEX_CODE_MAX_PX (default 2048) while classify + non-code stay at the 1024 base; IndexRequest.max_px overrides the client default for a whole build (reproduces the sweep from GUI/MCP).",
      "acceptance_criteria": [
        "Auto-path code/terminal frames are extracted at the higher max_px while slides/meeting/video stay at the base (verified: a code frame's structured_image is called with the bumped max_px).",
        "IndexRequest.max_px overrides the client default end-to-end (daemon builds the client with it).",
        "Hermetic + smoke + contract tests stay green; docs/specs/indexing.md updated."
      ],
      "passes": true,
      "dependencies": [
        44
      ]
    },
    {
      "id": 50,
      "category": "indexing",
      "priority": "high",
      "description": "Enrich the built-in `meeting` extractor with work content. Study (432498 standup): the default meeting schema (participants/active_speaker/shared_content) missed the user's primary interest — task assignments + data. Add task_assignments, data_points, decisions to CONTENT_PROMPTS['meeting'] schema+prompt so auto-classified meetings capture owner->task, ticket refs, dates, and decisions directly (auto already routes real Meet captures to `meeting`).",
      "acceptance_criteria": [
        "CONTENT_PROMPTS['meeting'] schema includes task_assignments/data_points/decisions and the prompt asks for them.",
        "combine_focus still attributes who-said-what.",
        "Hermetic/smoke/contract green; spec updated."
      ],
      "passes": true,
      "dependencies": [
        44
      ]
    },
    {
      "id": 51,
      "category": "indexing",
      "priority": "medium",
      "description": "Hybrid OCR-escalation, v1 (flag-now half — auto-re-read deferred per user). _flag_code_reliability post-processes code leaves: sets data.ocr_uncertain where the OCR'd file name is a singleton amid several differently-named code leaves (the local model's confabulation signature; validated to flag exactly the fake names on a real index, leaving real recurring files alone), and attaches data.narration_values (numbers/identifiers spoken in the frame's transcript) so the consumer prefers DICTATED tokens over garbled OCR (1c0c0d finding). AGENTS.md (#57) lists the flagged frames as 're-read these first'. DEFERRED: automated higher-res/frontier re-read of flagged frames at build time.",
      "acceptance_criteria": [
        "Detect cross-frame token instability for code content and mark those frames for re-read.",
        "Narration-dictated tokens deterministically win over conflicting OCR.",
        "Does not regress non-code content."
      ],
      "passes": true,
      "dependencies": [
        49
      ]
    },
    {
      "id": 52,
      "category": "gui",
      "priority": "medium",
      "description": "Index endpoint config as a structured PROVIDER + host:port (not a raw chat URL). Built a provider selector (LM Studio / Ollama / OpenAI / Custom) with per-provider default port + auth (key field only for providers that need one); the daemon composes the endpoint from provider+host+port (a full endpoint URL still works, back-compat). core/providers.py is the shared source of truth; GET /v1/index/providers; GUI Settings persists provider/host/port/key to gui-settings.json (legacy index_url migrated). GUI<->MCP parity via capture_index(provider/host/port).",
      "acceptance_criteria": [
        "Settings accepts host:port; daemon composes the endpoint path; existing full-URL config still works (back-compat).",
        "No provider selector yet, but the model is structured so one can be added.",
        "Parity: same config reachable from MCP/daemon."
      ],
      "passes": true,
      "dependencies": [
        44
      ]
    },
    {
      "id": 53,
      "category": "gui",
      "priority": "medium",
      "description": "Model selector as a DROPDOWN of the provider's available models. GET /v1/index/models lists the provider's /v1/models (providers.list_models); the GUI populates a dropdown (refreshed on provider/host/port change or a Refresh chip) and persists the choice; MCP index_models tool for parity; graceful empty list + reachable:false when unreachable.",
      "acceptance_criteria": [
        "Daemon route lists models from the configured provider; GUI shows a dropdown populated from it.",
        "Reachable from MCP/daemon (parity).",
        "Graceful when the endpoint is unreachable (empty list + hint)."
      ],
      "passes": true,
      "dependencies": [
        52
      ]
    },
    {
      "id": 54,
      "category": "gui",
      "priority": "medium",
      "description": "Start-capture PRESET picker. On Start, show a popup of capture presets so the capture meets indexing requirements: e.g. 'Meeting' -> enable mic + screenshots + index preset=meeting (+ task fields from #50); 'Coding tutorial' -> high max_px (#49) + code-aware prompt; 'Lecture', 'General'. The preset wires both capture-time config (mic/screenshots) and the index defaults for that session. Parity with MCP capture_start.",
      "acceptance_criteria": [
        "Start opens a preset popup; choosing a preset sets the matching capture + index config (e.g. meeting toggles mic on).",
        "The chosen preset is recorded on the session and used as the index default.",
        "MCP capture_start accepts the same preset for parity."
      ],
      "passes": true,
      "dependencies": [
        49,
        50
      ]
    },
    {
      "id": 55,
      "category": "engine",
      "priority": "high",
      "description": "Live/online incremental indexing during capture. core/live_index.py builds the index leaf-by-leaf via a binary MERGE-TREE (append → O(log n) new combines, never recomputes; validated 2N-1 nodes/single root across sizes). A daemon worker (start_live_index, gated on a reachable index endpoint remembered from the GUI's /v1/index/status probe) drives it off the capture hot path, checkpoints partial index.json+AGENTS.md mid-capture, and finalizes on stop; no endpoint → the existing post-capture build_index. SSE live_index/live_index_done. Integration-tested: worker captions frames incrementally + finalizes a valid tree.",
      "acceptance_criteria": [
        "With an endpoint configured, a live session accrues index nodes during capture (not only after stop).",
        "No endpoint -> unchanged post-capture behavior.",
        "Live indexing runs off the capture/daemon hot path; SSE reports progress; GUI<->MCP parity."
      ],
      "passes": true,
      "dependencies": [
        44,
        49
      ]
    },
    {
      "id": 56,
      "category": "indexing",
      "priority": "high",
      "description": "Fold the 6-eval research into the BUILT-IN classifier + extractors (was only partly done via #49/#50). CLASSIFY_PROMPT now classifies by the CONTENT shown, not the window chrome — the #1 recurring finding was that every screen-recorded YouTube/Twitch capture mis-routed to `video` and lost all code/algorithm detail; now a recording OF an IDE is `coding`, OF a tutorial is `lecture`, OF a call is `meeting`, and `video` is reserved for entertainment media. The `coding` extractor gained a verbatim `code` field (legible now that #49 extracts code frames at 2048) and `lecture` gained `code`+`formulas`.",
      "acceptance_criteria": [
        "A screen-recorded coding tutorial classifies as coding/lecture (not video) on the auto path.",
        "coding/lecture extractors return verbatim code (+ formulas for lecture).",
        "Hermetic/smoke/contracts green; indexing spec updated."
      ],
      "passes": true,
      "dependencies": [
        44,
        49
      ]
    },
    {
      "id": 57,
      "category": "indexing",
      "priority": "high",
      "description": "Generate a per-capture AGENTS.md after indexing — a trust-calibration + usage guide for the agent that later receives the capture artifact. The eval study proved the local 9B confabulates verbatim code (invented class names on UE5, fake fns on Strudel), so structured `data` fields (esp. `code`) are SCAFFOLDING not ground truth. The AGENTS.md (written by build_index into <session>/) states: what the capture is (root summary, duration, content-type mix); the artifact map (index.json tree, transcript.jsonl, screenshots/, index_prompts.json); and content-aware reliability rules grounded in the research — verbatim `code` is OCR-hallucination-prone (cross-check the transcript, RE-READ the high-res frame at repr_frame.path for exact tokens); narration is reliable and DICTATED values beat OCR; cross-frame disagreement flags unreliable regions; small-font shared-board text (ticket IDs) may be misread. Makes the cheap verbatim extraction safe to consume.",
      "acceptance_criteria": [
        "build_index writes <session>/AGENTS.md with the artifact map + reliability rules.",
        "The guidance is content-aware (code captures emphasize verbatim-code caution + re-read; meetings emphasize transcript-authoritative).",
        "A downstream agent reading only AGENTS.md + index.json knows which fields to trust vs verify."
      ],
      "passes": true,
      "dependencies": [
        44,
        56
      ]
    },
    {
      "id": 58,
      "category": "asr",
      "priority": "high",
      "description": "User-chosen ASR runtimes via installable runtime packs (owner directives 2026-06-17). NO engine bundled by default; the user picks a runtime matching their hardware (faster-cpu / faster-cuda NVIDIA / remote OpenAI-compat+Riva; AMD/Intel via whisper.cpp-Vulkan or ONNX-DirectML DEFERRED as registry slots — 'option 3 track'), the app downloads a prebuilt ABI-matched pack into %LOCALAPPDATA%/Capture/runtimes/<id>, core/asr/runtimes.activate() prepends it to sys.path + adds its DLL dirs so the FROZEN daemon imports the engine, then the user picks a compatible model and downloads it. NO silent fallback: device is explicit per runtime; load failures surface (audio_status / GET /v1/asr/backend), never a quiet CPU degrade. KEYSTONE VALIDATED 2026-06-17: a lean frozen daemon (faster-whisper excluded) imported an external CPU pack (ctranslate2 4.8 + faster-whisper 1.2.1, 286MB) via sys.path -> 'faster-whisper OK'. Decisions: frozen daemon + packs (not an embeddable venv); NVIDIA+CPU+remote now, AMD deferred. SPEC: docs/specs/asr-runtimes.md. [2026-06-17] LANDED: core/asr/runtimes.py (registry + activate/install/set_active/status_payload/backend_report); daemon routes GET /v1/asr/runtimes, POST /v1/asr/runtimes/install (background+SSE), POST /v1/asr/runtime, GET /v1/asr/backend (+ DaemonClient methods); NO-silent-fallback wired (FasterWhisper device from the runtime, raises on load failure -> GET /v1/asr/backend). Verified: install(local pack)->set_active->backend reports faster-cpu/cpu; routes live over HTTP; smoke 67/67, contracts 4/4. [2026-06-17 b] ALSO LANDED: packaging/build_runtime_packs.ps1 (builds dist/runtime-<id>-<pytag>.zip — faster-cpu = 86MB zip verified); build_windows.ps1 now freezes LEAN (no engine bundled); GUI runtime picker (Settings -> Voice recognition: list runtimes + GPU hint -> Install w/ SSE progress -> Use -> runtime-aware model picker; daemon.rs AsrRuntimes + 3 methods); pack_url() defaults to the GitHub release assets for the version. GUI builds + renders (RENDERER_OK). Remaining for passes:true: HOST the packs as release assets (release flow) + a release build/install verify; AMD runtimes (whisper.cpp/ONNX).",
      "acceptance_criteria": [
        "A fresh install has no ASR runtime; the GUI shows 'choose a runtime' and capture still records screenshots+logs (audio_status reflects 'no runtime', not silent).",
        "Selecting a runtime downloads+installs its pack; the frozen daemon then imports the engine (runtime_available true) and the model picker shows that runtime's compatible models to download.",
        "A chosen GPU runtime that can't load (e.g. no NVIDIA GPU / missing CUDA) surfaces a clear error and does NOT silently fall back to CPU."
      ],
      "passes": false,
      "dependencies": [
        34
      ]
    },
    {
      "id": 59,
      "category": "v3",
      "priority": "high",
      "passes": true,
      "dependencies": [],
      "description": "v3 SPIKE A — ScreenCaptureKit from Rust: DONE, GREEN. The safe `screencapturekit` v7 crate mirrors audiocap.swift ~1:1 (zero unsafe); all 3 ran (listed 309 windows, real PNG screenshot, per-app audio→16k s16le). No Swift needed. Packaging: pin apple-metal=0.6.0 + embed Swift-runtime rpath. Risk HIGH→LOW. Code: spikes/sck-capture/."
    },
    {
      "id": 60,
      "category": "v3",
      "priority": "high",
      "passes": true,
      "dependencies": [],
      "description": "v3 SPIKE B — whisper-rs vs mlx: DONE, ADOPT. whisper-rs 0.16 (metal) built clean, Metal on M3 Max, correct transcription at ~73x realtime (vs mlx-turbo ~33x). One crate covers mac/win/linux. Follow-ups: ship large-v3-turbo, warm context at start, chunk audio ourselves, ggml model cache. Code: spikes/whisper-bench/."
    },
    {
      "id": 61,
      "category": "v3",
      "priority": "high",
      "passes": false,
      "dependencies": [],
      "description": "v3 workspace skeleton + capture-core contract types. Stand up the cargo workspace; define the /v1 + on-disk CONTRACT TYPES in capture-core (serde) as the source of truth (replacing the pydantic v1_schema golden) and regenerate the GUI's types from them. Keeps the contract firewall stable for the incremental port."
    },
    {
      "id": 62,
      "category": "v3",
      "priority": "high",
      "passes": false,
      "dependencies": [
        61
      ],
      "description": "v3 capture-index port (reqwest+serde+image). Port vision_client + build_index + live_index (merge-tree) + AGENTS.md + the #49/#51/#56/#57 logic to Rust; `image` replaces sips for downscale/encode. Pure logic + HTTP — validate against the 7 existing eval corpora as regression fixtures (no capture/permissions needed). First high-value, low-risk port; proves the toolchain."
    },
    {
      "id": 63,
      "category": "v3",
      "priority": "high",
      "passes": false,
      "dependencies": [
        61,
        62
      ],
      "description": "v3 capture-daemon (/v1 axum + SSE) + capture-mcp (rmcp/JSON-RPC stdio). Serve the byte-identical /v1 + the 12-tool MCP surface from Rust; flip the GUI's daemon client to the Rust daemon. Validate against the contract + the GUI end-to-end."
    },
    {
      "id": 64,
      "category": "v3",
      "priority": "high",
      "passes": false,
      "dependencies": [
        60,
        61
      ],
      "description": "v3 capture-asr — pluggable AsrBackend trait + manager (mirrors asr/base+manager). whisper-rs default (Metal/CoreML on Mac, CUDA on Win), the OpenAI-compat/Nemotron HTTP backends, MLX optional-later behind the trait. Port the chunking + silence-gate + language/chunk settings. Benchmark-gated (spike B) before deleting the Python ASR."
    },
    {
      "id": 65,
      "category": "v3",
      "priority": "high",
      "passes": false,
      "dependencies": [
        59,
        61
      ],
      "description": "v3 capture-platform macOS — ScreenCaptureKit (app audio + screenshots) + AVFoundation mic via objc2, behind the WindowFinder/ScreenGrabber/AudioSource traits. Replaces audiocap.swift (capture moves in-process via FFI; the helper binary disappears). Gated on spike A."
    },
    {
      "id": 66,
      "category": "v3",
      "priority": "medium",
      "passes": false,
      "dependencies": [
        61,
        65
      ],
      "description": "v3 capture-platform Windows (windows-rs: Graphics.Capture + WASAPI loopback) + UNIFIED single-binary bundling. Replaces audiocap_win.py + the Win32 grabber. A single signed .exe + installer (cargo-dist/WiX) — no Python, no DLL zoo (the core motivation)."
    },
    {
      "id": 67,
      "category": "v3",
      "priority": "low",
      "passes": false,
      "dependencies": [
        62,
        63,
        64,
        65,
        66
      ],
      "description": "v3 cutover — retire the Python package + the Swift/Python helpers, drop PyInstaller; finalize cargo-dist packaging for mac+win. selenium→chromiumoxide (or keep optional) for the browser-capture flow. v3 becomes the default branch."
    }
  ]
}
