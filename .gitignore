.venv/
__pycache__/
*.pyc
helper/audiocap
*.egg-info/
dist/
build/
.DS_Store

# Notarization / signing secrets — LOCAL ONLY, never commit
.asp.capture
.notary-password
.notary-creds
*.secret
agent/build/

# transient capture session output (anchored to repo root so it does NOT match
# .claude/skills/capture-* skills)
/capture-*/
/_capture_raw/

# v3 cargo workspace build output (shared target at the repo root)
/target/

# v3 spike build artifacts
spikes/*/target/
spikes/whisper-bench/*.bin
